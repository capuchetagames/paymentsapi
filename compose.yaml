services:
  #Banco de Dados Sql Server
  payments-db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: payments_db_sql_server
    platform: linux/amd64
    user: root
    environment:
      ACCEPT_EULA: Y
      SA_PASSWORD: rooot1234!!
      MSSQL_PID: Developer
      #- MSSQL_USER=sa
    ports:
      - "1434:1433" 
    volumes:
      - payments_sql_data:/var/opt/mssql/data
    networks:
      - microsservicos-net
    healthcheck:
      test: >
        /opt/mssql-tools18/bin/sqlcmd
        -S localhost
        -U sa
        -P "rooot1234!!"
        -C
        -Q "SELECT 1"
      interval: 10s
      timeout: 5s
      retries: 10

  payments-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: payments_api_container
    restart: on-failure
    ports:
      - "5109:8080" 
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: >
        Server=payments-db;
        Database=Db.Payments;
        User Id=sa;
        Password=rooot1234!!;
        TrustServerCertificate=True;
      Jwt__Key: IkV6/I+/6WtC4xTRDjLMdwWl0jkt6UAU2R0NUwsL7fI=
    depends_on:
      payments-db:
        condition: service_healthy
    networks:
      - microsservicos-net

volumes:
  payments_sql_data:

networks:
  microsservicos-net:
    #external: true
    name: app-network
    driver: bridge