apiVersion: v1
kind: Service
metadata:
  name: payments-api
  namespace: fiapstore
  labels:
    app: payments-api
spec:
  type: LoadBalancer
  selector:
    app: payments-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
