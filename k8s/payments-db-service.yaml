apiVersion: v1
kind: Service
metadata:
  name: payments-db
  namespace: fiapstore
  labels:
    app: payments-db
spec:
  type: ClusterIP
  selector:
    app: payments-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
